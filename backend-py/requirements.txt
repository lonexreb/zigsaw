# Zigsaw backend-py — Python sidecar (issues #2, #3)
# Ported from zigsaw-backend; trimmed to what each ported module actually
# imports. Add deps when adding more services.

fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

httpx>=0.27.0
anthropic>=0.40.0
firebase-admin>=6.5.0

# MCP — Claude GitHub agent
mcp>=0.9.0

# Optional integrations (uncomment as services land)
# google-api-python-client>=2.150.0
# firecrawl-py>=0.0.16
